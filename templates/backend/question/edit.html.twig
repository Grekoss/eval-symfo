{% extends 'backend/backend_layout.html.twig' %}

{% block titleAdmin %}MODIFIER{% endblock %}

{% block bodyAdmin %}
    <h1 class="backend-title primary-0 primary-text-4">{{ question.title|capitalize }}</h1>

    {{ include('backend/question/_form.html.twig', {'button_label': 'Modifier'} ) }}

    <a class="btn btn-secondary" href="{{ path('backend_question_index') }}" title="Retour à la liste"><i class="fas fa-backward"></i></a>

    {{ include('backend/question/_delete_form.html.twig') }}
{% endblock %}